<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% title %></title>
</head>
<body>
    <h1><%= header %></h1>
    <% if (locals.errorMsg) { %>
        <div class="error-msg"><%= locals.errorMsg %></div>
    <% } %>
    <form action="/join" method="POST">
        <label for="passcode">Passcode for Club:</label>
        <input type="text" name="passcode" id="passcode">
        <button type="submit">Try</button>
    </form>
</body>
</html>